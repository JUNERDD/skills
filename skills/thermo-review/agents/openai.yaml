interface:
  display_name: "Thermo Review"
  short_description: "Harsh structural quality report"
  default_prompt: "Use $thermo-review to perform an extremely strict structural quality review and write a Markdown report with recursive coverage, boundary-aware 350-line checks, and a gate recommendation."

policy:
  allow_implicit_invocation: false
