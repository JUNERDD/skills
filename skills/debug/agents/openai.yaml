interface:
  display_name: "Coverage-First Debug & Repair"
  short_description: "Prove, repair, verify, and clean up runtime bugs"
  default_prompt: "Use $debug to trace and repair this runtime bug with a validated coverage plan and evolving investigation ledger, format every visible handoff as scannable Markdown sections, verify the original failure contract in a separate run, and clean up temporary instrumentation."
