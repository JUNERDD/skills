interface:
  display_name: "Coverage-First Debug & Repair"
  short_description: "Prove, repair, verify, and clean up runtime bugs"
  default_prompt: "Use $debug to trace and repair this runtime bug with a validated coverage plan, request each runtime run from me by default, automatically open the dashboard for browser-capable local user handoffs, use agent reproduction only for runs I explicitly delegate, maintain one ledger, and clean up temporary instrumentation."
