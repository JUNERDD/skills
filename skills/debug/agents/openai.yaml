interface:
  display_name: "Coverage-First Debug"
  short_description: "Coverage-first runtime root-cause debugging"
  default_prompt: "Use $debug to build and validate a coverage-first probe plan, collect one clean failing run, and prove the root cause from correlated runtime evidence."
