interface:
  display_name: "Multitask Coordinator"
  short_description: "Coordinate scoped subagent work"
  default_prompt: "Use $multitask-coordinator to coordinate a complex multi-step task, define safe worker scopes and isolation, synthesize results, and verify final acceptance."
