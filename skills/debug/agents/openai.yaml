interface:
  display_name: "One-Shot Runtime Debug"
  short_description: "Maximize root-cause coverage per reproduction"
  default_prompt: "Use $debug to maximize first-pass root-cause coverage, add correlated structured probes, analyze one clean reproduction, prove the fix, and clean up temporary instrumentation."
