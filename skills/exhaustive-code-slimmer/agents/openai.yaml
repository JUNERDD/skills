interface:
  display_name: "Exhaustive Code Slimmer"
  short_description: "Maximize AST-informed safe code deletion with DX-aware gates"
  default_prompt: "Use $exhaustive-code-slimmer to exhaustively shrink this codebase while preserving behavior, using AST-first evidence where available and pausing for approval before architecture-level refactors."
