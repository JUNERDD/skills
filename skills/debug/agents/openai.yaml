interface:
  display_name: "Runtime Debug"
  short_description: "Evidence-first root-cause debugging workflows"
  default_prompt: "Use $debug to instrument causal boundaries, prove the root cause from runtime evidence, repair the causal mechanism at the responsible ownership boundary and across every causally necessary layer, verify separately, and clean up temporary probes."
