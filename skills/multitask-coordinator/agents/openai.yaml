interface:
  display_name: "Multitask Coordinator"
  short_description: "Coordinate complex subagent work"
  default_prompt: "Use $multitask-coordinator to decide whether to delegate a complex repo task, assign worker ownership boundaries, synthesize results, and verify the final outcome."
